<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>{$title}</title>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
	<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet"
	>
	<link href="{$basePath}/css/style.css" rel="stylesheet">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container">
		<a class="navbar-brand" href="{link Home:default}">Valorant Tournaments</a>
		<button class="navbar-toggler" type="button"
				data-bs-toggle="collapse" data-bs-target="#mainNav">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="mainNav">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link" href="{link Home:default}">Home</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{link Tournaments:default}">Tournaments</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{link Teams:default}">Teams</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{link Forum:default}">Forum</a>
				</li>
			</ul>
			<ul class="navbar-nav ms-auto">
				{if $user->isLoggedIn()}
					<!-- existing links… -->
					{if $user->isInRole('admin')}
						<li class="nav-item">
							<a class="nav-link" href="{link Admin:default}">Admin Panel</a>
						</li>
					{/if}
				{/if}
			</ul>

			<ul class="navbar-nav ms-auto">
				{if $user->isLoggedIn()}
					<li class="nav-item">
						<a class="nav-link" href="{link Teams:myTeam}">My Team</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{link Invite:list}">🔔 Notifications</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{link Logout:default}">Logout</a>
					</li>
				{else}
					<li class="nav-item">
						<a class="nav-link" href="{link Login:default}">Login</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{link Register:default}">Register</a>
					</li>
				{/if}
			</ul>
		</div>
	</div>
</nav>

<div class="container mt-4">
	{block content}{/block}
</div>

<script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>